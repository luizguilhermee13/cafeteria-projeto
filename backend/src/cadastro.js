/* const Singleton = require("../static/singleton");

function criarConta(userData) {
  const banco = Singleton.getvalue();
  banco.query("select *from estoque", (error, resultado) => {
    if (error) {
      console.log(error);
    }
    console.log(resultado);
  });
}
export default criarConta;
 */
